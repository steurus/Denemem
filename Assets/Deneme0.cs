using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Deneme0 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Branch Deneme");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
